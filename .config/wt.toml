# Worktrunk project config — shared with team
# https://worktrunk.dev

[pre-start]
# Bootstrap new worktrees by copying ignored files first (including node_modules cache)
# then running a fast pnpm install to reconcile links/lockfile.
bootstrap = "wt step copy-ignored && pnpm install"
