export interface ContentItem {
  id: string;
  originalText: string;
  languageCode: string;
  sourceSystem: string;
  sourceRecordId: string;
  createdAt: string;
  createdBy: string;
  // Add other fields as needed
}
