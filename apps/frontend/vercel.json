{
  "framework": "nextjs",
  "installCommand": "source ../../vercel-setup.sh && pnpm install --frozen-lockfile",
  "buildCommand": "source ../../vercel-setup.sh && if [ \"$VERCEL_ENV\" = \"preview\" ]; then (cd ../.. && pnpm seed:users); fi && next build",
  "crons": [
    {
      "path": "/api/cron/di-ingestion",
      "schedule": "0 2 * * 0-4"
    }
  ]
}
